# Project guideline overrides

Copy only rules that differ from the kit defaults into this directory. Use the
same role filenames as `guidelines/base/`. Page-specific deltas belong under
the page's own `guidelines/` directory.

