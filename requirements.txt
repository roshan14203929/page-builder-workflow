# Runtime dependencies for the Python implementation. Keep versions pinned so
# rendering and pixel-comparison results are reproducible across machines.
Pillow==11.3.0
pixelmatch==0.3.0
playwright==1.55.0
